Rplots.pdf
.Rhistory
.RData
.Rproj.user/

# Outputs generated by the analysis scripts (PR #2 review). Every file matched
# below is written by an activity*/step*/*.R script and can be regenerated by
# running those scripts in order, so committing them only inflates the repo.
# Scoped to the activity folders on purpose: reqs/ holds the assignment PDF and
# report/ will hold the submitted report, and neither is generated.
activity*/step*/*.pdf
activity*/step*/*.csv
activity*/step*/*.txt
activity*/dataset/*.rds
activity*/step*/*.rds
ExampleCode/
theories.md
final_report/
*.pdf
